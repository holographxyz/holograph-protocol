[profile.default]
solc_version = '0.8.13'
optimizer = true
optimizer_runs = 999999
out = 'artifacts'
test = 'test'
src = 'src'
libs = ['node_modules', 'lib']
bytecode_hash = "none"
gas_reports = ['*']
ignored_warnings_from = ["scripts"]

# [profile.default.fmt]
# line_length = 120
# bracket_spacing = true
# wrap_comments = true
# number_underscore = "thousands"
fs_permissions = [{ access = "read", path = "./artifacts" },{ access = "read", path = "./broadcast" }]

[rpc_endpoints]
# Mainnets
ethereum = "${ETHEREUM_RPC_URL}"
polygon = "${POLYGON_RPC_URL}"
avalanche = "${AVALANCHE_RPC_URL}"
bsc = "${BINANCE_SMART_CHAIN_RPC_URL}"
optimism = "${OPTIMISM_RPC_URL}"
arbitrum = "${ARBITRUM_RPC_URL}"
zora = "${ZORA_RPC_URL}"
mantle = "${MANTLE_RPC_URL}"
base = "${BASE_RPC_URL}"
linea = "${LINEA_RPC_URL}"

# Testnets
sepolia = "${ETHEREUM_TESTNET_SEPOLIA_RPC_URL}"
mumbai = "${POLYGON_TESTNET_RPC_URL}"
fuji = "${AVALANCHE_TESTNET_RPC_URL}"
bscTestnet = "${BINANCE_SMART_CHAIN_TESTNET_RPC_URL}"
opsepolia = "${OPTIMISM_TESTNET_SEPOLIA_RPC_URL}"
arbsepolia = "${ARBITRUM_TESTNET_SEPOLIA_RPC_URL}"
zorasepolia = "${ZORA_TESTNET_SEPOLIA_RPC_URL}"
mantleTestnet = "${MANTLE_TESTNET_RPC_URL}"
baseSepolia = "${BASE_TESTNET_SEPOLIA_RPC_URL}"
lineaSepolia = "${LINEA_TESTNET_SEPOLIA_RPC_URL}"

[etherscan]
sepolia = { key = "${ETHEREUM_TESTNET_API_KEY}", chain="11155111" }
# mumbai = { key = "${POLYGON_TESTNET_API_KEY}", chain="80001" }
# fuji = { key = "${AVALANCHE_TESTNET_API_KEY}", chain="43113" }
# bscTestnet = { key = "${BINANCE_SMART_CHAIN_TESTNET_API_KEY}", chain="97" }
opsepolia = { key = "${OPTIMISM_TESTNET_SEPOLIA_API_KEY}", chain="11155420"  }
arbsepolia = { key = "${ARBITRUM_TESTNET_SEPOLIA_API_KEY}", chain="421614"  }
# zorasepolia = { key = "${MANTLE_TESTNET_API_KEY}", chain="999999999"  }
# mantleTestnet = { key = "${LINEA_TESTNET_SEPOLIA_API_KEY}", chain="5001"  }
# baseSepolia = { key = "${BASE_TESTNET_SEPOLIA_API_KEY}", chain="84532"  }
# lineaSepolia = { key = "${ZORA_TESTNET_SEPOLIA_API_KEY}", chain="59141"  }
